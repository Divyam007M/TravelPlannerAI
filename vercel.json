{
  "version": 2,
  "comment": "Root-level Vercel config — tells Vercel the frontend lives in /frontend.",
  "buildCommand": "cd frontend && npm install && npm run build",
  "outputDirectory": "frontend/dist",
  "framework": null,
  "rewrites": [
    { "source": "/(.*)", "destination": "/index.html" }
  ],
  "env": {
    "VITE_API_URL": "@wander_api_url"
  }
}
